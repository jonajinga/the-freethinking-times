{#
  audio-bar.njk — global persistent TTS player. Lives at the bottom
  of every page (rendered from base.njk), hidden until something is
  loaded. Driven by assets/js/audio-bar.js.

  - On article pages, the inline article-audio player is the primary
    surface; the bar floats above the bottom annotation toolbar.
  - On non-article pages (cards listing, archives, reading list,
    knowledge map, etc.) the bar is the only audio surface.

  Click any [data-tft-audio-trigger] element with the right data-*
  attributes and audio-bar.js will load + play it here.
#}
{# Expose the manifest to client-side surfaces (reading-list,
   reading-history, print-basket, knowledge-map side panel) so
   they can render their own Listen buttons on demand. #}
<script id="audio-manifest-data" type="application/json" data-pagefind-ignore>{{ audioManifest | dump | safe }}</script>

<aside id="audio-bar" class="audio-bar" hidden aria-label="Article audio player" data-pagefind-ignore>
  <button type="button" class="audio-bar__play" id="audio-bar-play" aria-label="Play / pause">
    <svg class="audio-bar__icon-play"  width="14" height="14" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><polygon points="5 3 19 12 5 21 5 3"/></svg>
    <svg class="audio-bar__icon-pause" width="14" height="14" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true" hidden><rect x="6" y="4" width="4" height="16"/><rect x="14" y="4" width="4" height="16"/></svg>
  </button>
  <div class="audio-bar__meta">
    <a class="audio-bar__title" href="#" id="audio-bar-title" data-umami-event="audio-bar-open">Untitled</a>
    <span class="audio-bar__time">
      <span id="audio-bar-current">0:00</span>
      <span aria-hidden="true">/</span>
      <span id="audio-bar-duration">0:00</span>
    </span>
  </div>
  <input type="range" class="audio-bar__scrub" id="audio-bar-scrub" min="0" max="1000" step="1" value="0" aria-label="Seek">
  <button type="button" class="audio-bar__close" id="audio-bar-close" aria-label="Close audio player">
    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
      <line x1="18" y1="6" x2="6" y2="18"/>
      <line x1="6" y1="6" x2="18" y2="18"/>
    </svg>
  </button>
  <audio id="audio-bar-source" preload="none"></audio>
</aside>
